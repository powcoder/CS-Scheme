
(define (vv f g x y)
        ; add your code here

)

(define (vm f g x y)
        ; add your code here

)

(define (mv f g x y)
        ; add your code here

)

(define (mm f g x y)
        ; add your code here

)

